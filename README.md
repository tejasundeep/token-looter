# token-looter
TokenLooter is a headless FastAPI proxy gateway that provides thread-safe key rotation, sliding-window rate limiting, and instant failover across multiple LLM providers using simple JSON configurations.
